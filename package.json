{
  "name": "obsidian-knowledge-dashboard",
  "version": "1.1.0",
  "description": "Knowledge base dashboard for Obsidian - shows note counts by folder hierarchy",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.17.0",
    "obsidian": "latest",
    "typescript": "5.3.3"
  }
}
